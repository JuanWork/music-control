class CursoRepository {
  listar(filtros) { throw new Error('Método não implementado'); }
  buscarPorId(id) { throw new Error('Método não implementado'); }
  criar(curso) { throw new Error('Método não implementado'); }
  atualizar(id, curso) { throw new Error('Método não implementado'); }
  deletar(id) { throw new Error('Método não implementado'); }
}

module.exports = { CursoRepository };
